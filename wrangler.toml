name = "leap-international-corporate-services"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"

# 环境变量在 Cloudflare Dashboard → Pages 项目 → Settings → Environment variables 配置
# 本地开发用 .dev.vars（不要提交到 git）
